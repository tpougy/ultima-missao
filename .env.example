# Server-side (used by instant-cli only, never bundled into the app)
INSTANT_APP_ADMIN_TOKEN=

# Client-side (bundled into the app, Vite requires the VITE_ prefix to expose these)
VITE_INSTANT_APP_ID=
VITE_APP_PASSWORD=
VITE_ADMIN_PASSWORD=
